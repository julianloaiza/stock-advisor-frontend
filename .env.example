# ======================================================
# Stock Advisor Frontend - Configuración del Entorno
# ======================================================

# VITE_API_BASE_URL: La URL de la API backend a la que el frontend se conectará
# IMPORTANTE: Esto debe apuntar a tu instancia de backend en ejecución
# Si se ejecuta localmente con puerto predeterminado, usa http://localhost:8080
# Si usas despliegue con Docker Compose, usa http://backend:8080 dentro de los contenedores
VITE_API_BASE_URL=http://localhost:8080

# VITE_DEFAULT_LANGUAGE: Idioma predeterminado para la interfaz de usuario
# Opciones: EN (Inglés) o ES (Español)
# Esto se usará cuando no haya preferencia de idioma almacenada en el navegador
VITE_DEFAULT_LANGUAGE=EN